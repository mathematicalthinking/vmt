language: node_js
node_js: 
  - 10.8
services:
  - mongodb
install: 
  - npm install
  - npm --prefix ./client install ./client
  - npm run build
cache:
 directories:
    - ~/.npm
    - ~/.cache
before_script:
  - node ./bin/www -- --silent &
  # - node ./client/scripts/start.js &
script: 
  - cypress run